# Project rules

- Do not scaffold new projects (`ng new`, `create-*`, etc.) or run project setup commands. The user creates and initializes all projects themselves. Wait for the project to exist, then help with code inside it.
- Do not edit, create, or delete files unless I explicitly say "implement", "make the change", "edit the file", or similar. If I'm just discussing an approach, describe it in words or plan — don't touch code until I confirm.
