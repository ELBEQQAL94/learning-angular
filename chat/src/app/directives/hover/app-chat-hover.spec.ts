import { AppChatHover } from './app-chat-hover';

describe('AppChatHover', () => {
  it('should create an instance', () => {
    const directive = new AppChatHover();
    expect(directive).toBeTruthy();
  });
});
